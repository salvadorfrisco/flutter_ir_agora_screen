name: flutter_ir_agora_screen
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  intl: ^0.18.0
  http: ^1.1.0
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.8  # Necessário para gerar os mocks
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: Titillium
      fonts:
        - asset: assets/Titillium-Regular.otf